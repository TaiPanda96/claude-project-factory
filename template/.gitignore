# Secrets — never commit these
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
secrets/
credentials/

# Pipeline handoff files — ephemeral, session-scoped, not part of history
tmp/

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
